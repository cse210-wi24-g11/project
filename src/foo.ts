/**
 * @overview
 * this file exists to have a sample file with associated tests so we don't get ci errors about no tests existing
 */

export function add(a: number, b: number): number {
  return a + b
}
